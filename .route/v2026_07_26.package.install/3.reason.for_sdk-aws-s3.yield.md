# reason: sdk-aws-s3@0.2.0

## package
- name: sdk-aws-s3
- version: 0.2.0
- type: prep

## reason
s3 global-tier acceptance test proves the vision headline (s3 = global isolation): withSimpleMutex serializes across machines over a real s3-backed conditional-write cache. mutex stays cache-agnostic so this is a prep/test-only devDep, wired as the cloud `via` adapter in the acceptance suite. reuses the shared ehmpathy-sdk-aws-s3-test-bucket under a namespaced prefix.
