# 5.3.verification — with-simple-mutex mvp

> buttonup gate. proof that every promised behavior has a test, no skips exist, and all six
> suites pass — each cited with the exact command + observed output.

---

## verification checklist

### behavior coverage (vision journeys → acceptance proof)

every behavior the vision/readme promises maps to an acceptance case with a snapshot.

| journey (from vision) | test | snapshot? | critical path? | ergonomics ok? | status |
|-----------------------|------|-----------|----------------|----------------|--------|
| per-process tier (in-memory) serializes shared key | index.journey `case1` | ✓ | ✓ frictionless | ✓ natural | ✅ |
| per-machine tier (on-disk) — same call, swap cache | index.journey `case2` | ✓ | ✓ | ✓ | ✅ |
| global tier (s3) serializes across the cloud | index.s3.journey `case1`–`case3` | ✓ | ✓ | ✓ | ✅ |
| key sanitization (flagship `proxy-phone.+1463…`) | index.journey `case4` + asSafeMutexKey unit | ✓ | ✓ | ✓ | ✅ |
| out-of-bounds lease → ConstraintError | index.journey `case9` | ✓ | ✓ | ✓ | ✅ |
| out-of-bounds acquire.interval → ConstraintError | index.journey `case9b` | ✓ | ✓ | ✓ | ✅ |
| acquire.timeout does NOT bound a hung backend call (v1 limit) | index.journey `case9c` | ✓ | ✓ | ✓ disclosed | ✅ |
| non-positive lease → ConstraintError | index.journey `case9d` | ✓ | ✓ | ✓ | ✅ |
| reentrant self-deadlock (keyed, not owner-aware) | index.journey `case10` | ✓ | ✓ | ✓ disclosed | ✅ |
| crash mid-work → native expiration reclaims key | index.journey `case11` | ✓ | ✓ | ✓ | ✅ |
| lease expires mid-work → SimpleMutexLeaseExpiredError (lease lock, not fenced) | index.journey `case12` | ✓ | ✓ | ✓ | ✅ |
| many callers race one key → serialized | index.journey `case13` | ✓ | ✓ | ✓ | ✅ |
| distinct cache instances grant no shared lock (footgun) | index.journey `case14` | ✓ | ✓ | ✓ disclosed | ✅ |
| run exactly once — try-once acquire (timeout=0) skips a held key | index.journey `case15` | ✓ | ✓ | ✓ | ✅ |
| plain (non-conditional) cache → compile error | withSimpleMutex.type.test.ts | n/a (type) | ✓ pit-of-success | ✓ | ✅ |
| logic throws → lock released, error propagates (no failhide) | withSimpleMutex.integration | ✓ | ✓ | ✓ | ✅ |
| release-fault sequence (success vs failure path) | withSimpleMutex.integration + delMutexLock.integration | ✓ | ✓ | ✓ | ✅ |
| acquire-path faults shaped loud (put + version-read) | genMutexLock.integration | ✓ | ✓ | ✓ | ✅ |

no behavior promised by the vision/readme is left untested.

### zero skips verified
- [x] no `.skip()` / `.only()` / `xit` / `xdescribe` found (grep across `src/` → no matches)
- [x] no silent credential bypasses (`if (!cred) return` — none; the one `toBe(true)` is a real
  `isIsoTimeStamp` assertion, not `expect(true).toBe(true)`)
- [x] no prior failures carried forward (all suites 0 failed)

### snapshot coverage for contract outputs

the public contract is the `withSimpleMutex` wrapper (an sdk-grain function). its user-faced outputs
are the pass-through result and the three typed errors — each snapshotted at the acceptance grain.

| contract output | variants | snapshot file | status |
|-----------------|----------|---------------|--------|
| success pass-through | in-memory, on-disk, s3, at-scale | index.journey + index.s3.journey `.snap` | ✅ |
| ConstraintError (bad lease/interval) | over-max lease, over-max interval, non-positive | index.journey `.snap` (case9/9b/9d) | ✅ |
| SimpleMutexAcquireTimeoutError | reentrant timeout, try-once skip | index.journey `.snap` (case10/case15) | ✅ |
| SimpleMutexLeaseExpiredError | lease lapses mid-section | index.journey `.snap` (case12) | ✅ |
| acquire-path faults (Malfunction) | put fault, version-read fault, lease-too-short | genMutexLock.integration `.snap` | ✅ |
| release-fault composition | success-path + combined-fault | withSimpleMutex.integration `.snap` | ✅ |

- [x] every sdk output variant is exercised (success, each error, edge cases)
- [x] snapshots demonstrate actual output, not just "it ran" (each pairs a snapshot with explicit
  by-value assertions — see rule.require.snapshots)

### snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| index.journey.acceptance.test.ts.snap | added (case9d, case15) | yes | new lower-bound + try-once cases minted this verification round's fixes |
| index.s3.journey.acceptance.test.ts.snap | prior | yes | live s3-tier proof (unchanged this round) |
| genMutexLock.integration.test.ts.snap | prior | yes | acquire-path fault contracts (unchanged this round) |
| withSimpleMutex.integration.test.ts.snap | prior | yes | release-fault composition (unchanged this round) |

- [x] every `.snap` change reviewed; the two additions (case9d, case15) are intended coverage, minted
  deterministically (volatile `waited Nms` masked to `<mse>ms`)

### tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, passed 9s, no errors |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, passed 1s, no errors |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, passed 18s (biome + cycles + depcheck) |
| unit | `rhx git.repo.test --what unit --mode apply` | ✅ | exit 0, 54 passed / 0 failed / 0 skipped (12 suites) |
| integration | `rhx git.repo.test --what integration --mode apply` | ✅ | exit 0, 60 passed / 0 failed / 0 skipped (4 suites) |
| acceptance | `rhx git.repo.test --what acceptance --against local --env test --mode apply` | ✅ | exit 0, 78 passed / 0 failed / 0 skipped (2 suites) |

- [x] every command was run (not assumed), output observed, exit code verified 0
- [x] no test skipped, mocked at a forbidden grain, or faked; integration/acceptance use real caches
  (in-memory, on-disk, live s3) with only sanctioned fault-injection at boundaries

### contract output snapshot exhaustiveness

| contract | positive path | negative path | edge cases |
|----------|---------------|---------------|------------|
| `withSimpleMutex` (sdk) | ✓ (case1/2/13 + s3) | ✓ (Constraint/AcquireTimeout/LeaseExpired) | ✓ (case9/9b/9c/9d, case10, case11, case14, case15) |

- [x] every user-faced output has a snapshot for success, error, and edge cases
- [x] zero gaps in caller experience — the reviewer sees exactly what callers see

### blockers
- none. all six suites pass; every vision behavior is covered + snapshotted; zero skips.

---

## git-capture note

all deliverable files (`src/**`, readme, behavior route) are staged in the index (`git status` → all
`A`, zero `??`). the commit itself is the downstream commit step, gated on a foreman-granted quota
(`git.commit.uses` → not yet set); the work tree is complete and index-captured, so no work is at
risk of loss.
