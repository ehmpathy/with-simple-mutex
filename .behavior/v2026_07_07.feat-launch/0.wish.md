wish =

launch the mvp version of with-simple-mutex implementation, based on this handoff


the readme has the best picture of what we'd like to create

but here's an older proposal from a peer. (readme superceeds this, but it may help ground)


---

                                      
  ---
  with-simple-mutex — readme spec (handoff)
                                                                                             
  title / one-liner
                                                                                             
  ▎ a simple distributed mutex over any conditional-write cache — pick your isolation tier   
  (process / machine / global) by picking your cache.
                                                                                             
  sections, in order                                                  

  1. .what

  one paragraph: withSimpleMutex runs a critical section while a lock over a key is held, so 
  concurrent holders serialize. it takes any cache that advertises WithCacheConditionals
  (from with-simple-cache), so the isolation scope is inherited from the cache backend —     
  in-memory = per-process, on-disk = per-machine, s3 = global/cross-machine.

  2. .why

  - serialize access to a shared resource across processes/machines (the motivating case:    
  stop two integration test suites from a shared-external-account collision).
  - correctness comes from the cache's atomic conditional write — no settle-then-verify hack,
   no timing assumptions.                                                                    
  - one primitive, three isolation tiers, chosen by the caller.
                                                                                             
  3. install                                                                                 
  
  npm install with-simple-mutex                                                              
                                                                      
  4. quickstart — show all three tiers, same call                                            
  
  This is the headline of the readme. The only thing that changes is the cache.              
  import { withSimpleMutex } from 'with-simple-mutex';                
  import { createCache } from 'simple-on-disk-cache';                                        
  import { sdkAwsS3 } from 'sdk-aws-s3';                              
                                        
  // global (cross-machine) — s3-backed                                                      
  const cache = createCache({          
    directory: { cloud: { path: 's3://my-bucket/locks', via: sdkAwsS3 } },                   
  });                                                                     
                                                                                             
  const result = await withSimpleMutex(
    async () => {                                                                            
      // critical section — only one holder runs this at a time       
      return doTheThing();                                     
    },                                                                                       
    { key: 'proxy-phone.+14632270513', cache, ttl: { minutes: 5 } },
  );                                                                                         
  Then a short table:                                                                        
   
  ┌──────────────────────────────────┬────────────────────────┐                              
  │              cache               │    isolation scope     │       
  ├──────────────────────────────────┼────────────────────────┤                              
  │ simple-in-memory-cache           │ per-process            │       
  ├──────────────────────────────────┼────────────────────────┤
  │ simple-on-disk-cache (local dir) │ per-machine            │                              
  ├──────────────────────────────────┼────────────────────────┤
  │ simple-on-disk-cache (s3)        │ global / cross-machine │                              
  └──────────────────────────────────┴────────────────────────┘       

  5. the atomicity requirement (make it loud)                                                
   
  ▎ withSimpleMutex requires a cache that satisfies WithCacheConditionals. a plain           
  SimpleCache will not compile — this is deliberate: a lock without atomic writes is not a 
  lock.                                                                                      
                                                                      
  Show the condition interface it relies on:
  // the mutex builds on the cache's conditional write:
  cache.set(key, value, {                              
    condition: {                                                                             
      version: string | null,      // null = write only if absent; '<etag>' = write only if 
  unchanged                                                                                  
      exception: 'throw' | 'ignore', // on a precondition miss: throw a typed error, or no-op
    },                                                                                       
  });                                                                                        
                                                                      
  6. .how — the lifecycle, mapped to conditional writes                                      
                                                                                             
  Spell out the three ops so readers trust it:                                               
  - acquire → set(key, lock, { condition: { version: null, exception: 'ignore' } })          
  (put-if-absent); to steal an expired lease, read the current { value, version }, confirm   
  it's stale, then set(..., { condition: { version, exception: 'ignore' } }) (compare-and-set
   so a rival can't be clobbered).                                                           
  - renew (heartbeat) → set(key, lock, { condition: { version: mine, exception: 'ignore' } })
   — extend expiry only if still ours.                                                       
  - release → conditional delete on our version — remove only if still ours.                 
   
  7. api                                                                                     
                                                                      
  withSimpleMutex<T>(                                                                        
    logic: () => Promise<T>,                                          
    input: {
      key: string;
      cache: WithCacheConditionals<SimpleCache<string>>;
      ttl?: Duration;                                   // lease lifetime; default { minutes:
   5 }                                                                                       
      acquire?: { timeout?: Duration; interval?: Duration };                                 
    },                                                                                       
  ): Promise<T>                                                       
  Plus a small table documenting each field + default, and SimpleMutexAcquireTimeoutError.
                                                                                             
  8. .note — honesty section (keep it)
                                                                                             
  - it's a lease lock: if a holder stalls past ttl, another holder legitimately steals the   
  key. use a ttl comfortably above your critical section's worst case.                       
  - with conditional writes it is atomic on acquire (exactly one winner), unlike a           
  settle-then-verify approach — call this out as the upgrade over naive check-then-set.      
  - not a fenced lock: it does not hand out monotonic fencing tokens, so it does not defend a
   downstream store against a stalled-then-resumed holder. it makes contention safe and rare,
   not "impossible under arbitrary pauses."                           
                                                                                             
  9. see also                                                         

  link the contract + backends: with-simple-cache (WithCacheConditionals),                   
  simple-on-disk-cache, sdk-aws-s3.
                                                                                             
  ---                                                                 
  copy/tone notes for whoever writes it
                                       
  - lead with the "same call, swap the cache → swap the isolation tier" story; that's the
  package's whole pitch.                                                                     
  - the atomicity requirement is a feature, not fine print — frame the compile-time rejection
   of non-atomic caches as a pit-of-success.                                                 
  - keep the lease-vs-fenced honesty; don't oversell mutual exclusion.
                                                                                             
  ---                                                                 
  Two things to flag on the interface change you mentioned:                                  
                                                                      
  1. version: string | null collapses put-if-absent and CAS into one field — clean. Just
  confirm the readback story: acquire still needs a way to read { value, version } (a        
  getWithVersion or equivalent) so renew/release can pass the right version. Worth naming
  that read op in the see-also.                                                              
  2. exception: 'throw' | 'ignore' — for the mutex I'd default to 'ignore' on acquire/renew
  (a lost race is expected control flow, not an error), and reserve 'throw' for callers who  
  want hard failures. The readme's .how should show 'ignore' so readers don't think every
  contention throws.                                                                         
                                                                      
  Want me to also draft the matching SimpleMutexAcquireTimeoutError + .what/.why header      
  comments for the impl so they're consistent with this readme, or leave that until
  #5/#43/#57 actually land and we retype to WithCacheConditionals?                           
                                                                      
✻ Sautéed for 57s                                   

❯ yeah craft the mathcing comments                                                           
                                                         
● Here are the matching header comments — crafted for the atomic WithCacheConditionals       
  version so they line up with the readme. I'm giving them as a draft rather than applying
  them, because the live impl is still best-effort settle-then-verify, and stamping          
  atomic-contract comments onto settle-then-verify code would be a lie (comments describing
  behavior the code doesn't have). These drop in when we retype to WithCacheConditionals     
  after #57/#5/#43 land.                                              

  /**
   * .what = the error thrown when a lock cannot be taken within the acquire timeout
   * .why = a caller that cannot get the lock in its allotted window needs a typed,          
   *        catchable signal — distinct from an error thrown by the critical section         
   *        itself — so it can back off, fail fast, or escalate deliberately                 
   */                                                                                        
  export class SimpleMutexAcquireTimeoutError extends HelpfulError {}                        
                                                                                             
  /**                                                                 
   * .what = run a critical section while a distributed lock over a key is held, so          
   *         concurrent holders serialize instead of collide                                 
   * .why =
   *   - serializes access to a shared resource across processes and machines (the           
   *     motivating case: stop two integration test suites from a shared-external-           
   *     account collision)                                                                  
   *   - correctness rests on the cache's atomic conditional write, so exactly one           
   *     holder wins the key — no settle-then-verify, no timing assumptions                  
   *   - the isolation scope is inherited from the cache backend: in-memory locks a          
   *     process, on-disk locks a machine, s3 locks globally
   *                                                                                         
   * .how =                                                           
   *   1. acquire — put-if-absent via set(key, lock, { condition: { version: null,           
   *      exception: 'ignore' } }); a stale lease (older than ttl) is stolen with a          
   *      compare-and-set on its version, so a rival cannot be clobbered mid-steal           
   *   2. hold — a heartbeat renews the lease on an interval (compare-and-set on our         
   *      own version) so a live holder keeps the key while a crashed holder's lease         
   *      lapses and becomes takeable                                                        
   *   3. release — a conditional delete on our version frees the key, but only if it        
   *      is still ours                                                                      
   *
   * .note = this is a lease lock, not a fenced lock. it is atomic on acquire (one           
   *         winner), but it hands out no monotonic fencing token, so under a process        
   *         pause longer than the ttl a stolen-then-resumed holder cannot be fended         
   *         off at a downstream store. it makes contention safe and rare, not               
   *         impossible under arbitrary pauses. size ttl above the critical section's        
   *         worst-case duration.                                                            
   */                                                                 
  export const withSimpleMutex = async <T>(                                                  
    logic: () => Promise<T>,                                          
    input: {                                                                                 
      /**
       * the key to lock on; concurrent holders of the same key serialize                    
       */                                                                                    
      key: string;
                                                                                             
      /**                                                             
       * any cache that advertises conditional writes (WithCacheConditionals); the
       * cache backend also fixes the lock's isolation scope (process/machine/global)        
       */
      cache: WithCacheConditionals<SimpleCache<string>>;                                     
                                                                                             
      /**
       * lease lifetime; a lock older than this is treated as stale and takeable.            
       * default { minutes: 5 }. size it above the critical section's worst case
       */                                                                                    
      ttl?: Duration;
                                                                                             
      /**                                                             
       * how long to keep attempting acquisition, and how often to retry between
       * attempts, before a SimpleMutexAcquireTimeoutError is thrown                         
       */                                                                                    
      acquire?: { timeout?: Duration; interval?: Duration };                                 
    },                                                                                       
  ): Promise<T> => {                                                                         
  
  And the internal-lock shape comment, so the persisted value is self-describing:            
                                                                      
  /**
   * .what = the value persisted under a locked key                                          
   * .why = token proves a lease is ours across renew/release; lockedAtMse dates it
   *        so a lease older than the ttl can be recognized as stale and stolen              
   */                                                                                        
  interface SimpleMutexLock {                                                                
    token: string;                                                                           
    lockedAtMse: number;                                              
  }

  A few deliberate choices worth calling out:                                                
  - condition: { version: null, exception: 'ignore' } shown for acquire/renew — a lost race
  is expected control flow, so 'ignore' (no-op on miss), matching the readme's guidance.     
  'throw' is left for callers who want hard failures.                                   
  - The .note keeps the lease-vs-fenced honesty verbatim in spirit, but upgrades the old     
  "best-effort / could double-win" line to "atomic on acquire (one winner)" — because with
  WithCacheConditionals that's now true, whereas today's settle-then-verify only makes it    
  rare. That's exactly the sentence that must change in lockstep with the retype, not before.
  - lowercase, no gerunds, input-object with per-field doc comments — consistent with the    
  repo's rule.require.what-why-headers and rule.require.input-context-pattern.


----


defer to the readme on conflicts, as the readme has the updated contract we'd prefer if there is a divergence. (it was tuned more recently; e.g., ttl -> lease and etc)

