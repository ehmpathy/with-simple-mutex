✨ all clear
   ├─ logs: .log/bhrain/review/2026-07-26T05-23-05-158Z
   ├─ review: .behavior/v2026_07_07.feat-launch/.reviews/5.1.execution.from_vision.peer-review.ergo-friction-hazards.md
   └─ summary
      ├─ 0 blockers
      └─ 1 nitpicks 🟠

---
# nitpick.1: error messages lack actionable fix guidance

**rule**: rule.forbid.friction-hazards.md

**locations**:
- src/domain.objects/SimpleMutexAcquireTimeoutError.ts:14
- src/domain.objects/SimpleMutexLeaseExpiredError.ts:24

the error messages for SimpleMutexAcquireTimeoutError and SimpleMutexLeaseExpiredError describe what went wrong but do not tell the caller how to resolve the issue. for example, 'could not acquire the mutex lock within the acquire timeout' does not suggest increasing the acquire timeout or reducing contention. similar for 'the mutex lease expired while the critical section was still active' – it does not advise increasing the lease duration. adding a brief suggestion in the error message would reduce debugging friction for developers.

**snippet**:
```ts
export class SimpleMutexAcquireTimeoutError extends HelpfulError<{ key: string; waitedMse: number }> {}
```
